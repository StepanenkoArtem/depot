<div class="jumbotron">
  <%= form_with(model: product) do |form| %>
    <div class="form-group">
      <%= form.label :title, class: "form-text" %>
      <%= form.text_field :title, class: "form-control-sm" %>
      <% product.errors.full_messages_for(:title).each do |message| %>
      <%= simple_format message, class: "badge alert-danger" %>
      <% end %>
    </div>

    <div class="form-group">
      <%= form.label :description, class: "form-text" %>
      <%= form.text_area :description, class: "form-control", rows: 6 %>
      <% product.errors.full_messages_for(:description).each do |message| %>
        <%= simple_format message, class: "badge alert-danger" %>
      <% end %>
    </div> 

    <div class="form-group">
      <%= form.label :image_url, class: "form-text" %>
      <%= form.text_field :image_url %>
      <% product.errors.full_messages_for(:image_url).each do |message| %>
        <%= simple_format message, class: "badge alert-danger" %>
      <% end %>
    </div>

    <div class="form-group">
      <%= form.label :price, class: "form-text" %>
      <%= form.text_field :price %>
      <% product.errors.full_messages_for(:price).each do |message| %>
        <%= simple_format message, class: "badge alert-danger"%>
      <% end %>
      </ul>
    </div>

    <div class="actions">
      <%= form.submit class: "btn btn-success" %>
    </div>

    
  <% end %>
</div>